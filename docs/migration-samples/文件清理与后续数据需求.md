# 迁移样本文件清理与后续数据需求（2026-03-25）

## 本次已处理

- 已删除确定重复文件：
  - `docs/migration-samples/旧系统导出内容/erp_sku (2).sql`
- 删除依据：
  - 与 `erp_sku.sql` 关键特征一致（同表结构、同 AUTO_INCREMENT、同 dump 时间）
  - 视为重复下载副本，不再保留

## 发现的“同名近似文件但内容不同”

以下两组不能直接删除，属于同表不同时刻导出：

1) `erp_work_processes.sql` vs `erp_work_processes (2).sql`
- 两者 dump 时间不同（`2026-03-23` / `2026-03-24`）
- 建议后续迁移优先使用较新的 `erp_work_processes (2).sql`

2) `erp_sku_work_processes.sql` vs `erp_sku_work_processes (2).sql`
- AUTO_INCREMENT 和 dump 时间不同
- 建议后续迁移优先使用较新的 `erp_sku_work_processes (2).sql`

## 阿里云执行结果目录说明

- `docs/migration-samples/阿里云执行结果` 下主要是 `.xlsx` 与少量 `.txt`
- 其中 `.xlsx` 为二进制文件，无法在当前自动清理流程中做内容比对
- 目前先全部保留，避免误删可能的执行凭证/结果导出

## 继续迁移前还需要的数据（请补充）

为推进“多样本批量迁移”并减少人工判断，请提供 3 组订单样本（每组一个目录）：

- `01_order.tsv`
- `02_sku.tsv`
- `03_sku_work_processes.tsv`
- `04_sku_process.tsv`
- `05_sku_cost_detail.tsv`
- `06_work_process_dict.tsv`（若无可留空，但请说明）

建议目录命名：

- `docs/migration-samples/SAMPLE_A_<order_no>/`
- `docs/migration-samples/SAMPLE_B_<order_no>/`
- `docs/migration-samples/SAMPLE_C_<order_no>/`

建议样本类型：

1. 正常大货单（中大数据量）
2. 含多个工艺项目的订单
3. 能通过 `processes_id` 关联到工序字典的订单

补齐后可进入下一步：将现有 `import-al50-sample.js` 升级为可配置的批量样本导入脚本。
