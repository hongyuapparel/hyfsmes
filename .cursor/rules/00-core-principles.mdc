---
description: 全局硬规则（范围控制、根因修复、复用优先、变更确认）
alwaysApply: true
---

# 00 Core Principles（全局必遵守）

## 1) 修改范围

- 只修改用户明确要求的模块/页面，不顺带扩散改动。
- 如为达成目标必须改动未点名内容，先说明原因、范围、影响，获确认后再改。

## 2) 根因修复（禁止补丁思路）

- 优先采用根本方案，优先用框架/组件库官方 API 与配置。
- 禁止以补丁掩盖问题（如无必要的强制覆盖、绕过组件机制、临时兜底掩盖根因）。

## 3) 组件复用优先（前端）

- 新页面/新功能优先复用现有基础组件与业务组件。
- 可扩展现有组件时，优先扩展 `props` / `slots`，而不是复制一份新样式。
- 页面以“组装业务流程”为主，通用视觉下沉到可复用组件。

## 4) 同类交互视觉一致

- 抽屉、弹窗、表单区、工具栏、列表操作区等同类交互必须统一结构与视觉语言。
- 特殊业务必须偏离统一样式时，先说明“为什么不能复用”并获确认。

## 5) 需求执行前检查

- 先确认：用户目标、改动边界、是否允许关联改动。
- 完成后在回复中说明：主要改动模块、关键文件、潜在影响点。

## 6) Element Plus Radio 防回退

- `el-radio` / `el-radio-button` 禁止使用 `label` 充当选中值。
- 必须显式使用 `value` / `:value`。
- 新增或修改 radio/tab 后，需执行 `npm run check:el-radio-value`。
