---
description: 字段定义规范 - 同一字段全系统统一，可配置
globs: frontend/**/*.{vue,ts},backend/**/*.ts
alwaysApply: false
---

# 字段定义规范

## 原则

- **单一数据源**：同一业务字段（如「公司名称」）在全系统使用同一 `code`，保证显示一致
- **字段驱动**：表单、列表、筛选、排序均基于 `fields/*` 配置渲染，不硬编码
- **可扩展**：后续可迁移至系统配置（DB），支持增减、排序

## 结构

```
frontend/src/fields/
  types.ts           # FieldDefinition 类型
  customer-fields.ts # 客户字段
  index.ts           # 统一导出
```

## 使用方式

- 新增页面：在 `fields/` 下新增 `xxx-fields.ts`，定义 code/label/type/order
- 表单/表格：遍历 `FIELDS_SORTED` 渲染，不写死列或表单项
- 其他页面引用同一字段：`import { CUSTOMER_FIELDS } from '@/fields'`，用相同 code 取数

## 复用字段的展现形式一致

**同一字段在全站必须使用相同的 UI 组件与数据源，不得因页面不同而改用不同形式。**

- **产品分组 (productGroup / optionsKey: productGroups)**  
  - 展现形式：**树形选择**（`el-tree-select`）  
  - 数据源：`getSystemOptionsTree('product_groups')`  
  - 转换：用 `toProductGroupTreeSelect` 转为 `{ label, value, children }`，`value` 为完整路径（如 `"业务员 > 白人服装 > 连衣裙"`）  
  - 适用场景：筛选栏、新建/编辑表单等，凡用到产品分组的地方一律用树形下拉，禁止使用扁平 `el-select` + `getProductGroups()`  
- **其他复用字段**：若某字段在某一页已约定为特定展现形式（如树形、级联、带搜索的下拉等），后续任何新页面引用该字段时，**必须沿用同一展现形式**；有疑问时查 `fields/*.ts` 与已有使用该字段的页面（如客户管理、产品列表）。

## 禁止

- 禁止在页面中硬编码字段名、label、列顺序
- 禁止同一含义的字段在不同页面使用不同 code 或不同显示
- 禁止同一复用字段在不同页面使用不同的组件或数据源（如产品分组在 A 页用树形、在 B 页用扁平下拉）
