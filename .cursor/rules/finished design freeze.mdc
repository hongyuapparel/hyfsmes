---
description: 锁定已完成设计的关键交互模块（筛选区、订单操作记录等），不得随意修改
alwaysApply: true
---

# 已完成设计模块保护规则（不要随意改）

本规则用于集中管理「已完成设计、以后默认不再改动」的前端模块。  
除非用户在当前会话里明确说明「允许修改某个模块/字段/交互」，否则以下模块一律视为锁定状态。

## 目录

- 订单管理
  - 订单列表筛选区（`frontend/src/views/orders/list.vue`）
  - 订单列表卡片底部【操作记录】弹窗及入口（同上）
- 产品管理
  - 产品列表筛选区（`frontend/src/views/orders/products.vue`）
- 客户管理
  - 客户列表筛选区（`frontend/src/views/customers/index.vue`）

---

## 订单管理

### 1. 订单列表筛选区（`frontend/src/views/orders/list.vue`）

- 筛选条与输入框交互（组件类型、前缀文案、宽度自适应、何时显示字段名等）均为**已固化设计**。  
- 除非用户点名某个筛选项，否则禁止调整上述行为。

### 2. 订单列表卡片底部【操作记录】弹窗（`frontend/src/views/orders/list.vue`）

已确定的设计包括：

- 卡片底部的「操作记录」按钮及其触发逻辑。
- 弹窗结构：订单信息摘要（订单号 / 客户 / SKU）+ 时间轴列表。
- 时间轴字段：
  - 操作账号：显示用户中文名或用户名。
  - 操作类型：中文文案（创建草稿 / 修改 / 提交 / 审核 / 删除 / 复制为草稿等）。
  - 修改日期：只显示年月日。
  - 修改内容：中文描述，日期为年月日，状态名为中文。

未经用户授权，禁止改动以上字段结构、文案语言和日期展示格式。

---

## 产品管理

### 产品列表筛选区（`frontend/src/views/orders/products.vue`）

- 规则同「订单列表筛选区」：筛选组件类型、前缀文案、宽度自适应和显示时机视为锁定，非经用户授权不可修改。

---

## 客户管理

### 客户列表筛选区（`frontend/src/views/customers/index.vue`）

- 规则同「订单列表筛选区」：筛选组件类型、前缀文案、宽度自适应和显示时机视为锁定，非经用户授权不可修改。

