---
description: 数据字段与配置项约定（字段驱动、统一展示、只存 ID）
globs: frontend/**/*.{vue,ts},backend/**/*.ts
alwaysApply: false
---

# 10 Data & Config Conventions

## 1) 字段驱动

- 同一业务字段全系统使用同一 `code`（单一数据源）。
- 表单/列表/筛选/排序优先基于 `fields/*` 配置渲染，避免页面硬编码。

## 2) 配置项只存 ID

- 来自系统配置（system options 或同类配置）的字段，持久化只存 `xxx_id`（或 ID 数组）。
- 禁止持久化“名称/路径”等冗余字段；名称用于展示时按 ID 解析。
- 配置项改名后，历史数据应通过 ID 自动展示新名称。

## 3) 复用字段展现一致

- 同一字段在全站应保持同一组件形态与数据源。
- 已约定为树形/级联/可搜索下拉的字段，后续页面必须沿用，不得随意改成其他控件。

## 4) 新增配置项默认流程

- 后端：只新增 ID 字段并校验输入，剥离误传名称。
- 前端：下拉 `value` 使用 ID，提交 payload 只传 ID。
- 返回：若页面要显示名称，在接口层或前端通过 ID 解析。
