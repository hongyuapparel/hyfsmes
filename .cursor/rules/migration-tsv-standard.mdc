# 迁移统一规则：旧系统导出与本地导入（TSV 标准）

## 适用范围

- 适用于本项目所有「旧系统 -> 新系统」数据迁移工作（订单、供应商、SKU、工艺、客户等）。

## 统一导出模式（强制）

- 旧系统导出必须在 **Linux shell** 执行，不在 `mysql>` 交互提示符中执行。
- 统一使用：
  - `mysql -uroot -p -D <旧库名> -N -B -e "SQL" > /tmp/.../*.tsv`
- 默认导出格式为 **TSV（制表符分隔）**，与历史订单迁移保持一致。
- 仅在明确要求时，才允许使用 CSV。

## 导出 SQL 规范

1. `SELECT` 明确列顺序，首行需人工补充表头（或使用脚本补表头）。
2. 日期统一在 SQL 中格式化为字符串（例如 `%Y-%m-%d %H:%i:%s`）。
3. 文本列使用 `IFNULL(col, '')`，避免 NULL 影响导入。
4. 导出前先 `mkdir -p /tmp/<migration_folder>`，并确认路径可写。
5. 导出后必须执行：
   - `wc -l <file.tsv>`
   - `sed -n '1,5p' <file.tsv>`
   - `sed -n '$p' <file.tsv>`

## 本地导入规范

- 本地导入脚本优先读取 `docs/migration-samples/**` 下的 TSV 文件。
- 导入执行顺序统一：
  1) `--dry-run` 预览  
  2) 正式导入  
  3) 导入后抽样核对页面与数据库

## 字段映射原则

- 严格按当前迁移任务约定字段映射，不得擅自扩展导入范围。
- 配置型下拉项遵守「只存 ID」规则：
  - 业务表仅存 `xxx_id` 或 ID 数组；
  - 展示名称通过配置表解析；
  - 若旧值在新系统不存在，按迁移约定补齐配置项后再入库。
