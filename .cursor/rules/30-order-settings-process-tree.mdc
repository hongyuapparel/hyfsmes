---
description: 设置页即时生效（按需读 docs + 工序树专项）
globs: frontend/src/views/settings/**/*.vue,frontend/src/views/**/settings/**/*.vue,frontend/src/api/**/*.{ts,js}
alwaysApply: false
---

# 30 Settings Live Update

改设置/配置页时，先读 `docs/AGENT_WORKFLOW.md` §设置页即时生效。

生产工序树（`order-settings.vue` 的 `productionProcesses`）专项：

- 「加载更多」仅影响当前父节点，不得重建整棵树
- 保持树表稳定键（`row-key="id"`）
- 保存后保证展开态与子层懒加载关系不被破坏
