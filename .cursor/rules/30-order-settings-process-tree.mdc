---
description: 设置中心通用即时生效规则 + 生产工序树专项补充
globs: frontend/src/views/settings/**/*.vue,frontend/src/views/**/settings/**/*.vue,frontend/src/api/**/*.{ts,js}
alwaysApply: false
---

# 30 Settings Live Update Rule

适用于所有“设置/配置项”页面与对应接口改动（如工序、供应商、仓库、类型、权限等）。

## A. 通用硬性要求（所有设置页必须遵守）

- 新增/编辑/删除后，当前界面必须立即可见结果，禁止依赖 F5 或手动刷新。
- 禁止用整页刷新或整块重建掩盖状态同步问题（如 `location.reload`、`router.go(0)`、外层动态 `key` 强制重建）。
- 优先局部更新当前列表/树/表单状态（patch 本地数据）；必须重拉时要恢复用户现场。
- 必须保留用户现场：筛选条件、分页、展开态、滚动位置、加载更多进度。
- 任何改动不得破坏“保存即显示”的既有页面能力；若影响旧页面，需一并修复。

## B. 提交前通用回归（改任一设置页必做）

- 新增后：列表/树立即出现新项，且定位与筛选逻辑正确。
- 编辑后：当前行/节点立即显示新值，不出现“提示成功但界面旧值”。
- 删除后：当前界面立即移除目标项，不残留脏数据。
- 页面不跳顶、不重置筛选/分页/展开；滚动位置基本保持。

## C. 生产工序树专项补充（`order-settings.vue` 的 `productionProcesses`）

- “加载更多”仅影响当前父节点，不得重建整棵树。
- 保持树表稳定键（`row-key="id"`），避免引入不稳定键类型。
- 保存后除即时更新外，必须同时保证展开态与子层懒加载关系不被破坏。
