---
description: 项目开发原则 - 修改范围、根因修复、变更确认
alwaysApply: true
---

# 项目开发原则

## 一、修改范围

**只修改用户明确要求修改的部分，不得擅自修改其他部分。**

- 用户要求改 A，就只改 A
- 不得以「顺带」「顺便」为由修改 B、C、D
- 若认为其他部分也需要调整，必须单独提出并征得同意后再改

## 二、根因修复（禁止补丁）

**任何实现需求的手段，都必须优先采用根本原因方案，严禁打补丁。**

- 适用范围：错误修复、样式调整、功能实现、第三方库使用等**所有场景**
- 优先使用库/框架提供的**官方 API**（如 Element Plus 的 `size`、`props` 等）
- 禁止：`!important` 覆盖、CSS 强制覆盖、绕过组件 API、用补丁掩盖问题

**示例：**
- ❌ 用 `!important` 或 `:deep()` 强制覆盖组件默认样式
- ✅ 使用组件的 `size="small"`、`props` 等官方配置

## 三、变更确认

**若为实现用户需求，必须改动用户未要求的部分，必须先提出方案并征得同意。**

- 先说明：为实现需求 X，需要修改 Y、Z
- 给出具体修改方案和影响范围
- 等待用户确认后再执行
- 未经确认不得擅自扩大修改范围

## 四、需求理解

在动手修改前，先确认：

1. 用户具体要改什么
2. 是否只改这一处
3. 是否允许为达成目标而改动其他相关代码（需先说明）

## 五、Element Plus Radio 约定（防回退）

- `el-radio` / `el-radio-button` **禁止**使用 `label` 充当选中值（如 `:label="xxx"`）。
- 必须使用显式 `value`（如 `:value="xxx"` 或 `value="xxx"`）。
- 任何新增或修改 tab/radio 代码时，提交前必须通过 `npm run check:el-radio-value`。
