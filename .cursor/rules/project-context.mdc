# 项目上下文与文档优先规则

> 目标：让 AI 在每个对话中都「记得」本项目的关键设计与历史决策，同时不额外制造太多规则负担。

---

## 一、文档优先与上下文加载

- **在对本项目做任何涉及业务逻辑、字段配置、订单流程的修改前**，助手必须先读取：  
  - `docs/PROJECT_CONTEXT.md`（项目全局上下文与已完成决策）；  
  - 若本次任务涉及「字段配置 / 列设置 / 字段显示与顺序」：同时读取 `docs/FIELD_CONFIG_DESIGN.md`；  
  - 若本次任务涉及「订单、订单状态、订单列表/编辑页、与生产相关流程」：同时读取 `docs/ORDER_STATUS_FLOW_DESIGN.md`。

- 若用户在对话中 @ 了其它文档（例如更多设计说明），优先按照文档中的约定实现，不擅自发明新的规则。

---

## 二、修改范围与同步更新（与全局开发原则协同）

在不与已有开发原则冲突的前提下，针对本项目补充以下约定：

- **修改范围控制**  
  - 严格遵守「只修改用户明确要求的模块/页面，不顺带扩散到其它模块」的原则。  
  - 若为实现需求，确实需要改动用户未点名的模块/文件，必须在当前对话中提前说明原因和影响，待用户确认后再改。

- **文档同步更新（轻量）**  
  - 当本次对话完成了**需求级别**的改动（例如新增模块、调整订单状态规则、统一字段配置、重构订单编辑页等），助手应：  
    - 在 `docs/PROJECT_CONTEXT.md` 的「已完成的重要功能与决策记录」中，增加一条一句话记录；  
    - 如有必要，可在对应设计文档（例如 `FIELD_CONFIG_DESIGN.md` / `ORDER_STATUS_FLOW_DESIGN.md`）补充或修正文案，但避免大面积重写，保持简洁。
  - 对于仅限样式微调、单个小 bug 修复等不影响业务逻辑的改动，可以不更新文档。

- **对话内改动说明**  
  - 每次完成一轮较大的代码修改后，在回复中简要列出：  
    - 本次主要改动的模块/页面名称；  
    - 涉及的关键文件路径（无需逐行说明）；  
    - 是否已同步更新相关文档（如有）。

> 通过以上规则，即使不同对话、不同时间在并行开发，也能依靠文档形成「持久记忆」，减少互相覆盖或改坏已确定方案的风险。

